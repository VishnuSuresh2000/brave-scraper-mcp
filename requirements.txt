# MCP Framework
mcp[cli]>=1.26.0

# HTTP Transport (Phase 7 - Docker)
starlette>=0.37.0
uvicorn>=0.30.0

# Browser Automation
patchright>=1.58.0

# Data Validation
pydantic>=2.0.0

# Async Support (for MCP)
anyio>=4.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Stealth / Anti-detection (Phase 2)
# Xvfb support is system-level (apt-get install xvfb)
# No additional Python packages needed for Xvfb

# CAPTCHA Solving dependencies (Phase 3)
pyautogui>=0.9.54
pillow>=10.0.0
pytesseract>=0.3.10
opencv-python-headless>=4.8.0

# Content Extraction (Phase 5)
trafilatura>=1.6.0
markdownify>=0.11.0
